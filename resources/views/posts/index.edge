@layout('main')

@section('content')

    <h3>Posts from index.edge template</h3>

    <p>{{title}}</p>
    @if(old('notification'))
        <div class="alert alert-success">
            {{old('notification')}}
        </div>
    @endif
    <ul class="list-group">
        @each(post in posts)
            <li class="list-group-item"> <a href="/posts/{{post.id}}">{{post.title}}</a> </li>
        @endeach
    </ul>

@endsection
